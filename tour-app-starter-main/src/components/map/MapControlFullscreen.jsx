import { FullscreenControl } from "react-map-gl";

const MapControlFullscreen = () => {
  return (
    <FullscreenControl
      style={{
        right: 8,
        top: 8,
      }}
    />
  );
};

export default MapControlFullscreen;
